#include <bits/stdc++.h>

using namespace std;

int low_binsearch(vector<int> arr, int toSearch){
	int low = 0;
	int high = size(arr) - 1;
	int mid = high / 2;

	while (low <= high){
		if mid = 
	}
	return e
}

int main(){
	vector<int> sortedArray = {0, 0, 0, 1, 1, 2, 6, 6, 9};
	return 0;
}
#include <bits/stdc++.h>
#include <functional>
#include <queue>
#include <vector>
using namespace std;

int szToTime(int sz){
    priority_queue<int, vector<int>, greater<int>> pq;
    pq.push(0);pq.push(0);pq.push(0);pq.push(0); // fuck
                                                 // times like this where i miss python
    
    



}

int main(){
    int c, t; cin >> c >> t;

    vector<int> arr(c);
    for (int i = 0; i < size(arr); i++){cin >> arr[i];}

    // stage sz -> time
}

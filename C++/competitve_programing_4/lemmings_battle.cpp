#include <bits/stdc++.h>
using namespace std;

int main(){
    int c; cin >> c;
    while (c--) {
        // set sorted from least to greatest
        int battles, greens, blues; cin >> battles >> greens >> blues;
        for (int i = 0; i < greens; i++);
        for (int i = 0; i < blues; i++);
    }
}

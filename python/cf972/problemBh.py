#!/bin/python3
import math

binSearch(arr, key):
	lo = 0
	hi = len(arr) - 1

	while hi > lo:
		mid = (hi + lo)

for c in range(int(input())):
	cells, teachers, numQuries = map(int, input().split())
	teacherLocations = list(map(int, input().split()))
	quries = list(map(int, input().split()))
	teacherLocations.sort()



